/**
 * Placeholder for Mongoose document model.
 * Will be implemented when MongoDB is integrated.
 */

// Example schema structure for future reference:
//
// import { Schema, model, models } from "mongoose";
//
// const DocumentSchema = new Schema({
//   name: { type: String, required: true },
//   type: { type: String, required: true },
//   size: { type: Number, required: true },
//   uploadedAt: { type: Date, default: Date.now },
//   status: { type: String, enum: ["processing", "completed", "failed"], default: "processing" },
// });
//
// export const DocumentModel = models.Document || model("Document", DocumentSchema);
